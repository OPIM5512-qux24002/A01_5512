# A01_5512
This project loads the California Housing dataset and creates a boxplot

This project uses the California Housing dataset from scikit-learn.

To run the script we use 3 commands: 
pip install -r requirements.txt
cd path/to/A01
python src/boxplot.py

The expected output of this project will be a saved boxplot image
